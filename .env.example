# MenuMiner Environment Variables
# Copy this file to .env.local and fill in your actual API keys

# ============================================
# Authentication (Google SSO)
# ============================================
# The email address allowed to access the application
# Only this Google account will be able to sign in
ALLOWED_EMAIL=your.email@example.com

# Google OAuth Client ID
# Get this from: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client ID for Web application
# Add authorized JavaScript origins: http://localhost:3000, https://your-domain.vercel.app
# Add authorized redirect URIs: http://localhost:3000, https://your-domain.vercel.app
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# For client-side (Vite requires VITE_ prefix)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# For development mode email check (Vite requires VITE_ prefix)
VITE_ALLOWED_EMAIL=your.email@example.com

# ============================================
# Gemini API Key
# ============================================
# Get your API key from: https://aistudio.google.com/app/apikey
# This is used for menu extraction from images using Google's Gemini AI

# Server-side (for production/Vercel)
GEMINI_API_KEY=your_gemini_api_key_here

# Client-side (for development - Vite requires VITE_ prefix)
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# ============================================
# Google Custom Search API
# ============================================
# Get your API key from: https://console.cloud.google.com/apis/credentials
# Enable the "Custom Search API" in your Google Cloud project
# This is used to fetch images for menu items

# Server-side (for production/Vercel)
GOOGLE_SEARCH_API_KEY=your_google_search_api_key_here

# Client-side (for development - Vite requires VITE_ prefix)
VITE_GOOGLE_SEARCH_API_KEY=your_google_search_api_key_here

# ============================================
# Google Custom Search Engine ID (CX)
# ============================================
# Create a Custom Search Engine at: https://programmablesearchengine.google.com/
# Configure it to search the entire web for images
# Copy the "Search engine ID" from your CSE control panel

# Server-side (for production/Vercel)
GOOGLE_SEARCH_CX=your_custom_search_engine_id_here

# Client-side (for development - Vite requires VITE_ prefix)
VITE_GOOGLE_SEARCH_CX=your_custom_search_engine_id_here

# ============================================
# Setup Instructions
# ============================================
# 1. Copy this file: cp .env.example .env.local
# 2. Get your Gemini API key from Google AI Studio
# 3. Create a Google Cloud project and enable Custom Search API
# 4. Create a Custom Search Engine for image search
# 5. Fill in all three values above
# 6. Run: npm run dev
#
# IMPORTANT: Never commit .env.local to version control!
# The .gitignore file is configured to exclude it.

# ============================================
# Security Warning
# ============================================
# WARNING: These API keys will be exposed in the client-side JavaScript bundle.
# This is acceptable for development and personal use, but NOT for production.
#
# For production deployment, you MUST implement a backend proxy to hide API keys.
# See docs/CRITICAL_ISSUES_PLAN.md for implementation details.
#
# Recommended production architecture:
# - Backend server (Express, Next.js API routes, or serverless functions)
# - API keys stored server-side only
# - Client makes requests to your backend, not directly to Google APIs
# - Implement rate limiting, authentication, and monitoring server-side

